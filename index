<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Govt Cyber Security Complaint Portal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f9fafc;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    header {
      background: linear-gradient(90deg, orange, white, blue, green);
      padding: 15px;
      text-align: center;
      color: #000;
      font-weight: bold;
      font-size: 24px;
      border-radius: 6px;
    }
    .card {
      border-radius: 15px;
      box-shadow: 0px 4px 12px rgba(0,0,0,0.1);
    }
    .nav-link.active {
      font-weight: bold;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    Govt Cyber Security Complaint Portal
  </header>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light mt-2">
    <div class="container">
      <a class="navbar-brand fw-bold" href="#">Portal</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link active" href="#complaintSection">Submit Complaint</a></li>
          <li class="nav-item"><a class="nav-link" href="#blogSection">Cyber Security Blog</a></li>
        </ul>
        <button class="btn btn-outline-primary" data-bs-toggle="modal" data-bs-target="#authModal">Login / Sign Up</button>
      </div>
    </div>
  </nav>

  <!-- Complaint Section -->
  <div id="complaintSection" class="container my-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card p-4">
          <h4 class="mb-3 text-center">Submit a Complaint</h4>
          <form id="complaintForm">
            <div class="mb-3">
              <label class="form-label">Full Name</label>
              <input type="text" class="form-control" id="name" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Address</label>
              <textarea class="form-control" id="address" rows="2" required></textarea>
            </div>
            <div class="mb-3">
              <label class="form-label">Phone Number</label>
              <input type="tel" class="form-control" id="phone" pattern="[0-9]{10}" placeholder="10-digit number" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Government Proof (License/Aadhaar)</label>
              <input type="file" class="form-control" id="govtProof" accept=".pdf,.jpg,.jpeg,.png" required>
            </div>
            <div class="mb-3">
              <label class="form-label">Incident Type</label>
              <select class="form-select" id="incidentType" required>
                <option value="">-- Select --</option>
              
    <option value="phishing">Phishing</option>
    <option value="unauthorized">Unauthorized Access</option>
    <option value="data_breach">Data Breach</option>
    <option value="ddos">DDoS (Distributed Denial of Service)</option>
    <option value="spam_call">Spam Call</option>
    <option value="money_loss">Money Loss</option>
    <option value="threatening">Threatening</option>
    <option value="mitm">Man-in-the-Middle Attack</option>
    <option value="network_theft">Network Theft</option>
  </select>
</div>

              </select>
            </div>
            <div class="mb-3">
              <label class="form-label">Description</label>
              <textarea class="form-control" id="description" rows="3" required></textarea>
            </div>
            <div class="mb-3">
              <label class="form-label">Upload Evidence</label>
              <input type="file" class="form-control" id="evidence" accept=".pdf,.jpg,.jpeg,.png">
            </div>
            <button type="submit" class="btn btn-primary w-100">Submit Complaint</button>
          </form>
        </div>

        <!-- Complaint Progress Animation -->
        <div class="card mt-4 p-4">
          <h5 class="text-center mb-3">Complaint Process</h5>
          <div class="progress" style="height: 25px;">
            <div id="progressBar" class="progress-bar progress-bar-striped progress-bar-animated bg-success" 
                 role="progressbar" style="width: 0%">0%</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Blog Section -->
  <div id="blogSection" class="container my-5">
    <h3 class="text-center mb-4">Cyber Security Awareness Blog</h3>
    <div class="row g-4">
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>⚠️ Phishing Attacks</h5>
          <p>Phishing emails trick users into giving away passwords or bank details. 
             Always check the sender's email address, avoid clicking suspicious links, 
             and never share sensitive data via email.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>🔑 Strong Passwords</h5>
          <p>Use long, unique passwords for every account. Avoid birthdays or names. 
             Enable two-factor authentication wherever possible to add extra security.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>📱 Social Engineering</h5>
          <p>Hackers may pretend to be officials to steal your details. 
             Never give out OTPs, Aadhaar, or card details on calls. 
             Genuine agencies never ask for such data.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>💻 Public Wi-Fi Risks</h5>
          <p>Avoid logging into sensitive accounts on public Wi-Fi. 
             Hackers can intercept traffic. Use VPNs for added protection.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>🛡️ Software Updates</h5>
          <p>Outdated software has vulnerabilities. Keep your OS, browser, and apps updated 
             regularly to protect against latest threats.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>📂 Data Backup</h5>
          <p>Ransomware can lock your files. Regularly back up important documents to 
             external drives or secure cloud storage to stay safe.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Success Modal -->
  <div class="modal fade" id="successModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content text-center p-4">
        <h5 class="text-success">✅ Complaint Submitted Successfully!</h5>
        <p>Your complaint has been recorded. We will review and update you soon.</p>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>

  <!-- Login / Signup Modal -->
  <div class="modal fade" id="authModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content p-4">
        <ul class="nav nav-tabs" id="authTabs">
          <li class="nav-item"><a class="nav-link active" data-bs-toggle="tab" href="#loginTab">Login</a></li>
          <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#signupTab">Sign Up</a></li>
        </ul>
        <div class="tab-content mt-3">
          <div class="tab-pane fade show active" id="loginTab">
            <input type="email" class="form-control mb-2" placeholder="Email">
            <input type="password" class="form-control mb-2" placeholder="Password">
            <button class="btn btn-primary w-100">Login</button>
          </div>
          <div class="tab-pane fade" id="signupTab">
            <input type="text" class="form-control mb-2" placeholder="Name">
            <input type="email" class="form-control mb-2" placeholder="Email">
            <input type="password" class="form-control mb-2" placeholder="Password">
            <button class="btn btn-success w-100">Sign Up</button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const form = document.getElementById("complaintForm");
    const progressBar = document.getElementById("progressBar");

    form.addEventListener("submit", function (e) {
      e.preventDefault();

      // Animate progress bar
      let progress = 0;
      let interval = setInterval(() => {
        if (progress >= 100) {
          clearInterval(interval);
          new bootstrap.Modal(document.getElementById("successModal")).show();
        } else {
          progress += 20;
          progressBar.style.width = progress + "%";
          progressBar.textContent = progress + "%";
        }
      }, 400);
    });
  </script>
</body>
</html>
